# AI Chat Features

## Обзор

AI чат в приложении теперь поддерживает расширенный режим просмотра и прикрепление изображений для анализа с помощью multimodal моделей.

## Новые возможности

### 1. Расширенный режим (Full Screen)

**Описание**: Чат можно развернуть на весь экран для более комфортной работы с длинными диалогами и изображениями.

**Как использовать**:
- Нажмите кнопку с иконкой "Развернуть" (⤢) в заголовке чата
- Чат откроется на весь экран поверх основного интерфейса
- Для возврата нажмите кнопку "Свернуть" (⤡)

**Технические детали**:
- Состояние управляется через `chatExpanded` в `FrameworkApp`
- В расширенном режиме чат рендерится как `fixed inset-0` overlay
- Автоматически закрывается при клике на backdrop (мобильные устройства)

### 2. Прикрепление изображений

**Описание**: Возможность отправлять изображения в чат для анализа AI моделями с vision capabilities.

**Как использовать**:
1. Нажмите кнопку со скрепкой (📎) в области ввода
2. Выберите одно или несколько изображений (PNG, JPEG, WebP, GIF)
3. Изображения отобразятся в виде превью с возможностью удаления
4. Напишите текст или вопрос об изображении
5. Отправьте сообщение

**Ограничения**:
- Максимальный размер файла: 10 МБ
- Поддерживаются только изображения (image/*)
- Множественная загрузка поддерживается

**Технические детали**:
- Изображения конвертируются в base64 data URLs
- Хранятся в `ChatMessage.attachedFiles[]`
- Отправляются в OpenRouter API как multimodal content
- Формат API: `{ type: 'image_url', image_url: { url: 'data:image/jpeg;base64,...' } }`

## Архитектура

### Типы данных

```typescript
interface AttachedFile {
  id: string;
  name: string;
  type: string;
  base64: string;  // data URL format
  size: number;
}

interface ChatMessage {
  role: 'user' | 'model';
  text: string;
  timestamp: number;
  attachedContexts?: ContextSnippet[];
  attachedFiles?: AttachedFile[];  // NEW
}
```

### API Integration

Функция `makeAICall` в `@shared/api/openrouter.ts` обновлена для поддержки multimodal контента:

```typescript
// Автоматически конвертирует сообщения с файлами в multimodal формат
const history = messages.map(m => ({
  role: m.role,
  text: m.text,
  files: m.attachedFiles
}));

// API layer преобразует в OpenRouter формат:
{
  role: 'user',
  content: [
    { type: 'text', text: 'Что на этом изображении?' },
    { type: 'image_url', image_url: { url: 'data:image/jpeg;base64,...' } }
  ]
}
```

### UI Components

**ChatPanel.tsx**:
- `attachedFiles` state для хранения выбранных файлов
- `fileInputRef` для программного открытия file picker
- `handleFileSelect` для обработки загрузки файлов
- `fileToBase64` для конвертации File в base64
- Preview компонент для отображения миниатюр

**FrameworkApp.tsx**:
- `chatExpanded` state для управления режимом отображения
- Условный рендеринг: expanded overlay vs normal sidebar
- Передача `isExpanded` и `onToggleExpand` props в ChatPanel

## Иконки

Добавлены новые иконки в `@shared/ui/icons.tsx`:
- `ExpandIcon` - развернуть на весь экран
- `ShrinkIcon` - свернуть в обычный режим
- `PaperclipIcon` - прикрепить файл
- `ImageIcon` - индикатор изображения

## Совместимость с моделями

Для работы с изображениями используйте vision-capable модели:
- `openai/gpt-4o` (рекомендуется)
- `google/gemini-2.0-flash-001`
- `anthropic/claude-3.5-sonnet`
- `openai/gpt-4-vision-preview`

Модель выбирается в настройках AI (Settings modal).

## Примеры использования

### Анализ изображения
1. Прикрепите скриншот интерфейса
2. Спросите: "Какие проблемы UX ты видишь на этом экране?"

### Сравнение дизайнов
1. Прикрепите несколько вариантов дизайна
2. Спросите: "Какой вариант лучше и почему?"

### OCR и извлечение текста
1. Прикрепите фото документа или таблицы
2. Спросите: "Извлеки данные из этой таблицы в формате Markdown"

## Будущие улучшения

- [ ] Поддержка PDF файлов
- [ ] Поддержка аудио файлов
- [ ] Drag & drop для загрузки файлов
- [ ] Paste изображений из буфера обмена
- [ ] Сжатие изображений перед отправкой
- [ ] Streaming ответов для больших изображений
- [ ] История загруженных файлов
